{
  "name": "skribo",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time video call transcription",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.0.0",
    "npm": ">=10.0.0"
  },
  "scripts": {
    "dev:extension": "npm run dev --workspace=@skribo/extension",
    "dev:backend": "npm run dev --workspace=@skribo/backend",
    "dev:admin": "npm run dev --workspace=@skribo/admin",
    "dev": "concurrently --kill-others-on-fail --raw \"npm run dev:backend\" \"npm run dev:extension\"",
    "dev:stop": "pkill -f 'tsx watch' && pkill -f 'vite build' || true",
    "build": "npm run build --workspace=@skribo/shared && npm run build --workspace=@skribo/backend && npm run build --workspace=@skribo/extension",
    "build:extension": "npm run build --workspace=@skribo/extension",
    "build:backend": "npm run build --workspace=@skribo/backend",
    "build:admin": "npm run build --workspace=@skribo/admin",
    "pack:extension": "bash scripts/pack-extension.sh",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "type-check": "npm run build --workspace=@skribo/shared && npm run type-check --workspace=@skribo/backend && npm run type-check --workspace=@skribo/extension && npm run type-check --workspace=@skribo/shared && npm run type-check --workspace=@skribo/admin",
    "clean": "npm run clean --workspaces --if-present"
  },
  "devDependencies": {
    "@typescript-eslint/eslint-plugin": "^8.0.0",
    "@typescript-eslint/parser": "^8.0.0",
    "concurrently": "^9.1.0",
    "eslint": "^9.0.0",
    "prettier": "^3.2.0",
    "typescript": "^5.7.0"
  }
}
