# Backlog

Список задач livescribe. Идентификатор — `LS-NN`. Порядок внутри фазы = приоритет.
Правила — в [`CONVENTIONS.md`](CONVENTIONS.md). Курсор состояния — в [`PROGRESS.md`](PROGRESS.md).

## Сделано

- [x] LS-00 — Meet per-track WebRTC-пайплайн (коммит `ea04f9c`).
- [x] LS-00 — STT сведён к одному Deepgram (коммит `245436c`, ADR-0001).
- [x] LS-00 — dev-flow: скиллы + конвенции документации + релиз-ноуты.

## Фаза: инфраструктура и продукт (приоритет)

- [x] LS-06 — **Хостинг бэкенда.** Beget VPS (`45.147.176.79`), systemd `skribo-backend`,
  Caddy TLS, `wss://api.skribo.ru/ws` работает. ADR-0002 + `deploy`-скилл. (Хвосты: CORS,
  чистка manifest, хардненинг сервера.)
- [x] LS-07 — **Postgres + персистентность.** Prisma-схема + сохранение сессий/транскриптов
  (WS-сессия с токеном → `Meeting` + сегменты). Сделано в фундаменте кабинета. (Аудио в
  объектное хранилище — отдельный follow-up.)
- [x] LS-08 — **Кабинет (админка).** Стек — React+Vite+RR7+Radix+`*.module.scss` (как
  expeditor, не Tailwind), см. спеку `docs/superpowers/specs/2026-07-24-admin-cabinet-design.md`.
  Кабинет-MVP закрыт (sub-plans 1–4); деплой — отдельным пунктом ниже.
  - [x] sub-plan 1: фундамент — auth (email+пароль, JWT-cookie), `/api/auth`, `/api/tokens`,
    `/api/meetings` (list+detail). Ревью пройдено, hardening-фикс применён.
  - [x] sub-plan 2: SPA-шелл `packages/admin` (React/Vite/RR7/`*.module.scss`) +
    `/login`/`/register` + app-shell + `/settings` с токеном. Ревью пройдено.
  - [x] sub-plan 3: страница списка переговоров (поиск/сортировка/карточки).
  - [x] Привязка расширения ↔ кабинет (персональный токен в WS `start`); связь проверена
    сквозняком (запись из расширения → встреча в кабинете).
  - [x] sub-plan 4: карточка встречи (`MeetingDetailPage` — транскрипт по спикерам +
    заглушка анализа; карточки списка кликабельны). Браузер-проверено сквозняком.
  - [x] Деплой кабинета на **`app.skribo.ru`** — LIVE: Postgres на ВМ + миграции, прод-`.env`
    (JWT_SECRET/DATABASE_URL/WEB_ORIGIN/NODE_ENV), admin в `/var/www/skribo-admin`, Caddy vhost
    (статика + `/api`), TLS Let's Encrypt. Регистрация/вход/список работают в проде.
  - Лендинг — отдельно, позже.
- [x] LS-11 — **Нормальный логин в расширении.** Убран ручной ввод токена: попап расширения
  делает авто-подхват сессии кабинета (`/api/auth/me` → `/api/auth/extension-token`) с
  фолбэком на вход email+пароль (`/api/auth/extension-login`). Бэкенд-флоу и цепочка
  токен → `Meeting` → `/api/meetings` проверены curl/psql/WS-скриптом
  (`.superpowers/sdd/task-3-report.md`). ⚠️ Рендер попапа в реальном Chrome и кросс-доменный
  авто-подхват cookie — **ждут ручной проверки пользователем** (среда агента не может
  загрузить unpacked-расширение); шаги проверки — в отчёте и `docs/PROGRESS.md`.
- [x] LS-12 — **Восстановление пароля по email.** SMTP (nodemailer, провайдер в env, ADR-0007):
  модель `PasswordResetToken` (хеш, 1ч, одноразовый — атомарно), `POST /api/auth/forgot`
  (всегда 200, без энумерации) + `/api/auth/reset`, страницы кабинета `/forgot`+`/reset` + ссылка
  на логине. Финальное ревью (opus) → merge. ⚠️ Деплой: SMTP-креды + `APP_URL` в prod-`.env` +
  `prisma migrate deploy`. Верификация email при регистрации + rate-limit — follow-up.
- [ ] LS-09 — **Анализ переговоров.** LLM (OpenRouter, ADR-0006) поверх транскриптов. Под-проекты:
  - [x] **A — анализ в кабинете.** LLM-ядро `packages/backend/src/llm/` (OpenRouter-клиент +
    сборка транскрипта + analysis-профиль, юнит-тесты), `POST /api/meetings/:id/analysis`
    (саммари + action items, upsert `Analysis`, коды 503/404/400/502), панель в `MeetingDetailPage`
    (кнопка «Проанализировать»/«Перегенерировать»). Финальное ревью (opus) → merge. ⚠️ **Живая
    проверка + `OPENROUTER_API_KEY` на сервере — при деплое** (без ключа фича мягко выключена, 503).
  - [x] **B — live-саммари в расширении.** Кнопка «Саммари встречи» в виджете → тезисы (3-6) по
    текущему миту; токен-авторизованный `POST /api/live-summary` (`LLM_MODEL_LIVE`, эфемерно),
    cross-origin fetch через service worker. Финальное ревью (opus) → merge. ⚠️ Живая проверка +
    `OPENROUTER_API_KEY` — при деплое (без ключа — «Саммари пока не настроено», 503).
  - Дальше: ключевые моменты/темы, поиск по анализу, авто-анализ при завершении встречи.
- [ ] LS-13 — **Self-host RU-STT (GigaAM / Parakeet)** — *плановая, gated на триггеры*
  (см. ADR-0005: качество RU / 152-ФЗ / объём / офлайн). Приоритет GigaAM-v3 (MIT, лучший
  русский), альтернатива — Parakeet Nemotron-Streaming. Добавляется провайдером к
  `STTProvider` (GPU-микросервис). Ресёрч: `docs/research/2026-07-26-stt-models-parakeet-gigaam.md`.
  **Смок-проверка на облачных API выполнена (2026-08-06/07)** — без GPU, т.к. Parakeet/Nemotron
  доступен через Together AI. Результаты: `docs/research/2026-08-06-cloud-stt-smoke-results.md`,
  дизайн — `docs/superpowers/specs/2026-08-06-cloud-stt-smoke-design.md`. **Решение владельца:
  остаёмся на Deepgram, пока действует триал** (ADR-0005, раздел «Проверка облачных
  вариантов»); при пересмотре — сначала Together (тот же nova-3 в 5× дешевле), не GPU.
  - Закрыто смоком: качество русского трёх облачных провайдеров (nova-3/Nemotron/Whisper) на
    нашем аудио, отзывчивость (интервал между финалами, хвост после конца записи), стабильность
    6-минутной сессии, фактические цены.
  - Осталось для self-host пути: GigaAM в посегментном режиме на CPU (нарезка по VAD +
    `transcribe()` сегмента — стриминг больше не обязателен, см. ADR-0005), замер скорости
    `transcribe()` на дорожку и числа параллельных дорожек на CPU-VPS, гибрид «стриминг для
    экрана (Deepgram/Together) + батч для итогового транскрипта (GigaAM)» как возможная
    архитектура вместо выбора одного провайдера. WER с эталонной разметкой всё ещё не сделан
    (смок был на глаз, не бенчмарк).

- [x] LS-14 — **Триггер-слова → подсветка в расширении.** Секция «Триггеры» в content-виджете
  (`chrome.storage.local.skriboTriggers`); финальная реплика с триггером (по границе слова,
  регистронезависимо, кириллица) подсвечивается инлайн-стилями + виджет пульсирует (Web
  Animations API). Клиентское, без бэкенда/LLM. Коммиты `3cc066f`+`56b0705`, финальное ревью
  (opus) → merge. ⚠️ Визуальная проверка в Chrome — за пользователем.

## Фаза: транскрипт доезжает до кабинета (по обращению клиента 2026-08-05)

Разбор одного обращения («несколько сессий, внутри звонка пусто») вскрыл четыре независимых
дефекта. Подробности механик — в [`KNOWLEDGE.md`](KNOWLEDGE.md).

- [x] LS-22 — **Сегменты per-track не сохранялись вообще.** Запись в БД жила только в
  session-level STT-колбэке (mixed); в per-track у каждого участника свой провайдер со своим
  колбэком, и он только отдавал текст в виджет. Митинг создавался и закрывался с верной
  длительностью, `segments` пустой. Оба режима переведены на общий `persistFinalSegment`.
- [x] LS-23 — **Один звонок = одна запись.** Каждое переподключение WS создавало новый `Meeting`
  (в проде наблюдалось ~60 с; 31.07 звонок распался на 5 огрызков по 61 с). `start` принимает
  `resumeMeetingId`, бэкенд продолжает митинг владельца токена и держит исходный `startedAt` как
  начало отсчёта. Тем же механизмом склеивается ручная пауза (стоп → старт); идентичность звонка
  — `origin + path` URL, пауза рисуется в кабинете из разрыва `tsMs`.
  ⚠️ Проверка в браузере — за пользователем (нужна перезагрузка расширения).
- [x] LS-24 — **Токен расширения был на аккаунт, а не на устройство.** Вход на втором устройстве
  молча удалял токен первого; то продолжало посылать отозванный токен, транскрипция шла, в
  кабинет не попадало ничего. Это и была причина «Teams-звонков нет в кабинете». Токены больше не
  затираются, а нераспознанный токен возвращает клиенту `AUTH_INVALID_TOKEN` (расширение чистит
  мёртвый токен и переавторизуется) вместо тихой деградации.
- [x] LS-25 — **Свой микрофон не попадал в транскрипт (per-track).** MAIN-world хук был подписан
  только на `pc.addEventListener('track')` — событие только для **входящих** дорожек, а микрофон
  уходит через sender. Итог: собственная речь не транскрибировалась и не была видна даже в audio
  levels. Теперь исходящая сторона отслеживается по трём путям (`getUserMedia`,
  `addTrack`/`addTransceiver`, `sender.replaceTrack` — подмена на анмьюте), трек кладётся в реестр
  с `data-local="true"` и подписывается «Вы». Сделано для **Pachca и Meet**. Второго разрешения на
  микрофон не нужно — страница уже им владеет. Эталон логики с тестами —
  `per-track/core/local-track-hook.ts`; в хуки он **вложен inline**, потому что MAIN-world скрипт
  обязан быть без импортов (см. `KNOWLEDGE.md`). ⚠️ Проверка в браузере — за пользователем.
- [x] LS-27 — **«Выйти» в расширении не держалось.** `signOut()` чистил токен, но попап трактует
  «токена нет» как «никогда не входил» и на следующем открытии автодетектил живую сессию кабинета,
  выдавая новый токен — кнопка выглядела сломанной. Явный выход теперь запоминается
  (`skriboAuthOptOut`, модуль `popup/auth-optout.ts`), автодетект его уважает, а сбрасывают флаг
  только осознанные действия: вход паролем или новая кнопка «Войти через кабинет» (чтобы вернуться
  без пароля, пока сессия кабинета жива).
- [ ] LS-28 — **Реплики привязываются не к тому спикеру (Teams, mixed).** На звонке 06.08 активен
  был один участник, а текст подписан другим. Имя берётся из таймлайна смен
  (`websocket/speaker-timeline.ts`, допуск `SPEAKER_LOOKAHEAD_MS = 750`). Кандидаты: DOM-индикатор
  Teams запаздывает или указывает не на того; пересчёт `startSec` → wall-clock врёт накопительно;
  либо `speaker`-сообщения не доезжают. Нужен прогон в браузере с сопоставлением логов.
  ⚠️ **Сначала перепроверить на живом звонке — возможно, уже починено.** Кандидат «пересчёт
  `startSec` врёт накопительно» подтвердился и закрыт в LS-30 (`0511c43`, `067635b`), а
  источник самих реконнектов — в LS-31 (`2922870`, `f476111`). После этих правок на прогоне
  45 с реальной записи нет ни одного переподключения (было два), шкала монотонна. То есть
  механизм, ломавший привязку, устранён. Не начинать браузерный дебаг, пока не проверено, что
  дефект вообще воспроизводится.
- [ ] LS-29 — **Пауза в кабинете: писать факт «Стоп → Старт», а не угадывать по разрыву.**
  Сейчас пометка выводится из разрыва `tsMs` с порогом 60 с; порог владельцем принят, поэтому
  задача — уточнение, не срочное. Точный путь: при resume бэкенд знает и время остановки
  (`Meeting.endedAt` от предыдущего финала, читать до обнуления), и время возобновления.
- [ ] LS-26 — **Клиентский WS отваливается каждые ~60 с (per-track).** Серверная сторона идлы не
  рубит — проверено: 150 с молчания через Caddy, соединение живо. LS-23 делает разрывы
  безвредными для данных, но аудио в момент гэпа теряется. Подозрения: offscreen-документ с
  `reason: USER_MEDIA`, который в per-track не держит ни одного медиа-стрима, и keepAlive-аларм
  `periodInMinutes: 0.5` (Chrome клампит до 60 с). Нужен прогон в реальном браузере.
- [x] LS-30 — **🐞 Переподключение Deepgram обнуляет шкалу времени сегментов.** Найдено при
  смок-проверке облачных STT (`docs/research/2026-08-06-cloud-stt-smoke-results.md`):
  `tryReconnect()` в `packages/backend/src/stt/deepgram.ts` открывает новое WS-подключение к
  Deepgram при любом разрыве, а у новой сессии `start` считается от её собственного нуля — то
  есть `startSec` (`packages/backend/src/stt/types.ts`) после реконнекта больше не соответствует
  реальной позиции в звонке. Это подтачивает ту же атрибуцию реплик к спикеру по времени, что
  чинила LS-20 (таймлайн смен спикера ждёт монотонный `startSec`). Наблюдался сброс
  `audioPosSec` на обеих прогнанных записях (короткой `recording-62a7123a` и длинной
  `recording-f11102c6`), воспроизводится через `scripts/stt-smoke`
  (`npm run stt:smoke -- --provider deepgram`). На доске — карточка #51, метка `bug`.
  **Починено** (`0511c43`, `067635b`): `stt/stream-clock.ts` считает реально отправленные в
  сокет байты и при реконнекте фиксирует накопленную длительность как смещение новой сессии;
  `stt/connection-guard.ts` отбрасывает результаты от соединения, которое перестало быть
  текущим (иначе к ним применялось чужое смещение, и они дублировали текст). Проверено на
  записи `recording-f11102c6`: позиции сегментов строго монотонны, нарушений ноль.
  **Граница применимости:** SDK Deepgram не подтверждает доставку — `send()` не бросает
  исключение для чанка, физически не долетевшего при обрыве. Такой чанк уже учтён в смещении,
  но в новое соединение не переотправляется, поэтому на каждом сетевом сбое возможен сдвиг
  **вперёд** на доли секунды. Монотонность при этом сохраняется, откат к нулю исключён.
  Закрыть полностью нельзя без подтверждений от Deepgram, которых потоковый API не даёт.
- [x] LS-31 — **🐞 Преждевременный reconnect открывает второе соединение к Deepgram.** При старте
  сессии `processAudio` вызывается раньше, чем соединение открылось; `connectionOpen` ещё
  `false`, срабатывает `tryReconnect()` и создаётся второе соединение — первое при этом всё
  равно открывается (в инструментированном прогоне видны два `open` подряд). Последствия:
  лишний оплачиваемый стрим на каждую сессию и лишний сброс шкалы. Рядом: обработчики `error`
  и `close` в `stt/deepgram.ts` не проверяют актуальность соединения, поэтому событие от
  старого может сбросить `connectionOpen` у живого и спровоцировать ещё один реконнект —
  чинить вместе. Нужно различать состояния `connecting` / `open` / `closed` вместо одного
  булева. На доске — карточка #53, метка `bug`.
  **Починено** (`2922870`, `f476111`): состояния `connecting`/`open`/`closed` (`stt/connection-state.ts`),
  все обработчики проверяют актуальность соединения, `reconnectInProgress` убран как избыточный.
  Отдельно понадобился сторожевой таймер (`stt/connect-watchdog.ts`, 8 с): запрет реконнекта из
  `connecting` убрал побочный self-healing, который давал баг, и без таймера зависший хендшейк
  оставил бы сессию без распознавания навсегда. Проверено на записи: один `open`, ноль
  реконнектов (было два), шкала монотонна.
  **Наблюдение:** константа 8 с не проверена на плохой сети — при медленном TLS-хендшейке
  возможны ложные срабатывания (лишний реконнект, не потеря сессии). Последить за строкой
  `WATCHDOG_TIMEOUT` в логах прода (включается `STT_DEBUG_RECONNECT=1`); если срабатывает —
  вынести таймаут в env.

## Фаза: кабинет UX (референс Tactiq — `docs/research/tactiq-ui/`)

- [ ] LS-17 — **Тайминги + поиск в транскрипте** (кабинет, клиентское). `mm:ss` у реплик (из
  `tsMs`) + инпут поиска по транскрипту (фильтр/подсветка, клиентски). Быстрый win, без бэкенда.
  Спека `docs/superpowers/specs/2026-07-28-cabinet-search-transcript-ux-design.md`.
- [ ] LS-16 — **Глобальный поиск по встречам** (кабинет + бэкенд). Страница `/search` + пункт
  сайдбара; поиск встреч по названию **и содержимому транскрипта** (расширить `/api/meetings` q на
  `TranscriptSegment.text`, user-scoped), карточки + сниппет. Спека — там же. (FTS/участники/лейблы —
  follow-up.)

## Фаза: покрытие платформ

- [ ] LS-01 — **Zoom per-track.** MAIN-world WebRTC-хук, per-track transcriber и speaker-DOM
  для Zoom; включить capabilities (сейчас у zoom всё `false`, только mixed).
- [x] LS-02 — **Teams per-track — НЕ ПРИМЕНИМО.** Разведка зондом в живом звонке (2026-08-03,
  `teams.cloud.microsoft`, 3+ участника, три снимка за 10 мин) показала: Teams сводит звук
  **на сервере**. Один активный peer-connection, `audioReceivers: 1`, один `inbound-rtp` —
  `ssrc 67003`, `trackIdentifier: "mainAudio-67003"`. Решающий замер: **при смене говорящего**
  (Gevorg → Dmitriy, `domSpeaker` переключился корректно) `ssrc`/`mid`/число ресиверов не
  изменились, а пакеты продолжили расти на том же потоке (+17 297 за 10:17, `audioLevel`
  0.09 → 0.40 → 0.56). При per-track голос нового участника пришёл бы по своему `ssrc`.
  Разделять нечего: ресиверы создаются на каждого отправляющего (как у Meet/Pachca), и даже
  слотовый SFU дал бы 2–3 ресивера. Привязки трека к плитке тоже нет — `attributeMatches`
  пуст на 882 просканированных элементах, аналога `data-ssrc` от Meet у Teams не существует.
  Отчёт разведки — в `docs/KNOWLEDGE.md`.
  **Teams остаётся mixed + speaker-DOM** (capabilities уже так и настроены, код менять не надо).
  Теоретический путь — Teams Premium spatial audio, но это десктоп-клиент, не веб.
  - [x] Инструмент разведки: WebRTC-зонд в dev-сборке (секция «🔬 Research (dev)» в виджете,
    снимки + JSON). Спека — `docs/superpowers/specs/2026-07-31-webrtc-research-probe-design.md`.
    Переиспользуется для LS-01 (Zoom).
- [x] LS-19 — **Speaker-DOM Teams почищен.** `participantId` берётся из `data-tid` (UPN,
  переживает ре-рендер) с фолбэком на `data-acc-element-id`; плитки
  `data-stream-type="ScreenSharing"` пропускаются при поиске говорящего, а их формат
  `aria-label` («Общий контент от пользователя X» / «Content shared by X») разбирается
  отдельно — раньше такая подпись целиком уехала бы в имя спикера. Чистые функции покрыты
  тестами (`active-speaker-dom.test.ts`).
- [x] LS-20 — **Спикер по времени сегмента, а не по времени доставки.** Раньше реплике
  клеился тот, кто говорит в момент прихода результата от Deepgram; при лаге STT 0.5–3 с
  фраза уезжала не тому. Теперь: `STTResult` несёт `startSec`/`durationSec` (из `payload.start`
  / `payload.duration` Deepgram), сессия ведёт таймлайн смен спикера с **серверными** метками
  приёма (синхронизация часов не нужна), сегмент атрибутируется по времени начала с допуском
  750 мс на запаздывание DOM-индикатора. Фолбэк на прежнее поведение, если таймингов нет.
  Чинит Teams и Zoom (mixed); per-track не затронут — там спикер едет с дорожкой.
  Тесты: `speaker-timeline.test.ts` + `handler-speaker.test.ts`.
- [x] LS-21 — **`resolveAudioMode` решает по capabilities.** Был дефолт «если не mixed, то
  per-track» — для платформы без per-track (Teams, Zoom, неизвестный хост) это описывало
  несуществующий пайплайн. Теперь платформа без `supportsPerTrackAudioMode` всегда `mixed`.
- [ ] LS-03 — **Хардненинг авто-детекта платформы.** Проверить надёжность `platform-detector`
  на всех поддерживаемых доменах и обновить `manifest.json` matches.

## Фаза: устойчивость и дистрибуция

- [ ] LS-04 — **Deepgram error/reconnect UX.** Явная индикация ошибок STT и восстановления
  потока в виджете; ретраи с backoff.
- [x] LS-05 — **Упаковка расширения под Chrome Web Store.** Иконки-монограмма (16/48/128),
  store-таргет сборки (`EXT_TARGET=store`: сужены host_permissions/matches/web_accessible_resources,
  вырезаны `platform-research` и YouTube; dev-сборка не тронута), `npm run pack:extension` → zip,
  публичная страница `/privacy` в кабинете (LIVE `https://app.skribo.ru/privacy`), гайд
  `docs/guides/chrome-web-store.md`. Финальное ревью (opus) → merge. Реальный логотип/локализация
  — follow-up. **Сама подача не сделана** — статус и чеклист ведутся в
  `docs/guides/chrome-web-store.md` (там же блокеры: прод-ключи, проверка `dist-store`,
  тестовый аккаунт для ревьюера).
- [x] LS-15 — **🔒 Прод не пишет и не отдаёт сырое аудио.** Была утечка: бэкенд безусловно писал
  аудио звонков в `recordings/*.wav` и отдавал их публично без авторизации (`GET /recordings`,
  `/recordings/:filename`). Загейчено `recordingsEnabled()` (`NODE_ENV !== production`, ленивая
  проверка — dotenv грузит `.env` после init модуля): в проде запись и эндпоинты отключены, в
  dev/test — работают. Задеплоено, 54 старые записи удалены, `/recordings` на проде → 404.
- [x] LS-18 — **🐞 Виджет со Start снова показывается + настройка автопоказа.** Регрессия от
  LS-11: с появлением `default_popup` в манифесте Chrome перестал звать `action.onClicked`,
  а это был единственный путь к `createUIWidget()` — виджет и кнопка Start стали недостижимы.
  Кнопка «Показать / Скрыть виджет» переехала в попап (`TOGGLE_WIDGET_IN_ACTIVE_TAB` →
  `toggleWidgetInTab()` в service-worker, с фолбэком на инъекцию контент-скрипта; лейбл — по
  `CONTENT_WIDGET_STATE`). Автопоказ на страницах звонков — отдельная опция
  `skriboAutoShowWidget` (по умолчанию выкл), применяется при загрузке страницы и при
  включении тумблера. Сборки: `BACKEND=local|prod` независимо от `BUILD_TARGET`, цель
  `build:extension:dev-prod` → `dist-dev-prod/`.
- [x] LS-10 — **Ребрендинг livescribe → Skribo.** Механическое переименование
  идентификаторов пакетов на `@skribo/*` (были со старым префиксом), алиасы
  `vite.config`/`tsconfig`, упоминания в коде/живых доках. Имя репозитория на GitHub —
  отдельный follow-up (см. ADR-0004).
