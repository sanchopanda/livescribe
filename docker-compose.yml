services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: skribo
      POSTGRES_PASSWORD: skribo
      POSTGRES_DB: skribo
    ports:
      - "5432:5432"
    volumes:
      - skribo_pgdata:/var/lib/postgresql/data
volumes:
  skribo_pgdata:
